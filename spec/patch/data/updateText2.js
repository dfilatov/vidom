import { h } from '../../helpers';

export default {
    'name' : 'updateText2',
    'trees' : [
        h('span', { children : 'text' }),
        h('span', { children : 'text' })
    ],
    'patch' : []
};
